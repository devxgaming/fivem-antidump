-- add your script code here, this can't be dumped by Cheater!!!!!
-- warning: onStartResource, onStopResource event will no called in this file. you can add on public.lua to trigger after 2000 ms. for example

-- AddEventHandler('onResourceStart', function(resource)
--    if GetCurrentResouceName() == resource then
---       Wait(2000)
--        TriggerEvent('YourTriggerOnClient.lua') 
--    end 
-- end)
